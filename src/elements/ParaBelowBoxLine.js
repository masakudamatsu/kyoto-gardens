import styled from 'styled-components';
import PropTypes from 'prop-types';

import P from './P';

const ParaBelowBoxLine = styled(P)``;

ParaBelowBoxLine.propTypes = {
  message: PropTypes.bool,
};

export default ParaBelowBoxLine;
