import styled from 'styled-components';
import PropTypes from 'prop-types';

import {shippoPattern} from 'src/utils/patterns';

const MainRyoanji = styled.main`
  ${shippoPattern}
`;

MainRyoanji.propTypes = {};

export default MainRyoanji;
